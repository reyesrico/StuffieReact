# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

#Source is published, Azure can't find the default page. (Need to pay)
#name: Build and deploy Node.js app to Azure Web App - stuffier

#on:
#  push:
#    branches:
#      - master

# jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest

#    steps:
#    - uses: actions/checkout@master

#    - name: Set up Node.js version
#      uses: actions/setup-node@v1
#      with:
#        node-version: '12.x'

#    - name: npm install, build, and test
#      run: |
#        npm install
#        npm run build --if-present
#        npm run test --if-present

#    - name: 'Deploy to Azure Web App'
#      uses: azure/webapps-deploy@v1
#      with:
#        app-name: 'stuffier'
#        slot-name: 'production'
#        publish-profile: ${{ secrets.AzureAppService_PublishProfile_a289aa84d04b4bcb86670cd1249351c8 }}
#        package: .